{
  "type": "object",
  "properties": {
    "head": {
      "type": "object",
      "properties": {
        "request_time": {
          "title": "请求时间",
          "type": "string",
          "format": "date-time"
        },
        "user_id": {
          "title": "用户ID",
          "type": "string",
          "minLength": 1,
          "maxLength": 20,
          "pattern": "^[a-zA-Z0-9_@]+$",
          "patternDesc": "英数字"
        }
      },
      "required": ["request_time", "user_id"]
    },
    "body": {
      "type": "object",
      "properties": {
        "v_demandid": {
          "title": "需求方编码",
          "type": "string",
          "maxLength": 10,
          "description": "字符串的校验"
        }
      },
      "required": ["v_demandid"]
    }
  },
  "required": [
    "head",
    "body"
  ]
}